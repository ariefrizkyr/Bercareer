<div class="row">
  <div class="col-lg-12 text-center">
    <h1>Bercareer</h1>
    <p class="lead">This is the temp homepage</p>
    <%= link_to 'Create Resume', new_student_resume_path(current_student), class: 'btn btn-primary' if current_student %>
  </div>
</div>
